#include <bits/stdc++.h>
using namespace std;

bool ispar(string str){
    stack<char> st;
    for(int i=0;i<str.size()-1;i++){
        if(int i=0;)
    }
}


int main(){
    string str="{[][()]}";
    if(ispar(str)) cout<< "Balanced";
    else cout<<"not balanced";   
}